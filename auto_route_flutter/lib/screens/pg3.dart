import 'package:flutter/material.dart';

class Pg3 extends StatelessWidget {
  const Pg3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("PG3"),
      ),
    );
  }
}
